# Local HTTP port (default: 8080)
SERVER_PORT=8080

# PostgreSQL connection (replace with your local database values)
DB_URL=jdbc:postgresql://localhost:5432/bareum
DB_USERNAME=bareum
DB_PASSWORD=bareum-password
