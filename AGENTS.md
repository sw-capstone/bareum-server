# 프로젝트 공통 지침

## 목표

공개 API와 작업 상태를 관리하고, AI 처리 결과를 연결할 서버 경계를 제공한다.

## 기준

- 기획문서를 최우선 기준으로 사용한다.
- 첨부 하네스 구체화 문서는 명세·컨텍스트·권한·검증·피드백 설계에 사용한다.
- `결정 필요`, `팀 결정`, `멘토사 확인`, `검토` 항목은 임의로 확정하지 않는다.
- 충돌은 `docs/decisions/`에 쟁점·선택지·영향·재검토 조건으로 기록한다.

## 경계

- `bareum-web`은 이 저장소의 공개 API 계약만 소비한다.
- 백엔드의 기존 `src/main/`, `src/test/`와 Gradle 설정을 기준으로 작업하며 추가 모듈 경계는 관련 결정에 따른다.
- AI 처리 구현은 `bareum-server-ai`에서 관리하며 이 저장소에 AI 실행 경로를 중복해서 만들지 않는다.
- 영역 간 형식이 확정되면 `packages/contracts/`를 생성해 정의한다.
- 서버가 직접 소유하는 규칙과 계약 자산은 기준이 확정된 뒤 버전 추적한다. AI 처리 자산과 평가 구현은 `bareum-server-ai`의 기준을 따른다.
- 근거가 없거나 검증을 통과하지 못한 결과는 확정 판정으로 노출하지 않는다.

## GitHub와 ID

- GitHub 작업은 개발 가이드를 따른다.
- 공식 자산 ID 정책은 `docs/decisions/DEC-ID-001-id-governance.md`를 따른다.
- 하네스 검사와 정책은 `HAR-*` ID로 추적한다.

이 저장소는 `bareum-server`의 기준 루트다. 프론트엔드와 AI 구현을 이 저장소에 추가하지 않는다. 계약이 확정되면 `bareum-web`과 `bareum-server-ai`의 관련 소비자·제공자 검증을 함께 검토한다.
