# 판정

RMA가 라우팅한 규칙과 검색된 후보 근거의 범위 안에서만 구조화된 판정을 생성한다. 결정론으로 표현할 수 있는 형식·산술·존재 검사는 코드가 수행하고, LLM은 모호한 의미와 맥락 판단만 보조한다.

출력 Schema, 규칙 ID, 근거 실재와 근거 도출 검사를 통과하지 못한 결과는 판단보류 또는 비노출 처리한다. 카탈로그 심각도(`must_fix`, `verify`, `advisory`)와 사용자 출력 위험도(`high`, `medium`, `low`, `withheld`)를 혼합하지 않는다.
