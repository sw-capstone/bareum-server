# Trace

모델, 프롬프트 해시, 룰셋·카탈로그 버전, RMA 라벨 버전, 선택된 기준 데이터 snapshot, 외부 후보 근거, CBR 사례 승인 상태, 검증 결과, 재시도와 처리시간을 추적한다.

입력 위치 → 파서 블록 → RMA 라벨·라우팅 → 적용 규칙·검색 자료 → 모델 판단 → 후검사 → 사용자 노출·채택·기각의 연결을 보존한다. 사용자 원문과 비밀정보는 보존 정책에 따라 해시 또는 비식별 참조로 기록한다.
