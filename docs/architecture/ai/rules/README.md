# Rule Routing

제품 규칙 카탈로그를 읽고 문서 유형·RMA 라벨에 따라 검사 규칙을 라우팅한다.

- 규칙 ID는 `rules/` 카탈로그에 존재하는 값만 사용한다.
- 카탈로그에 없는 ID를 추측하여 생성하지 않는다.
- 카탈로그의 기본 심각도와 사용자 출력 위험도를 분리한다.
- 미검증 또는 `draft` 규칙은 실행 가능한 규칙으로 취급하지 않는다.
